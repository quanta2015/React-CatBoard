import React,{useState,useEffect} from 'react';import { observer,MobXProviderContext } from 'mobx-react'import classnames from 'classnames';import s from './index.module.less';import icon_user  from '@/img/icon/user.svg'import icon_eye   from '@/img/icon/eye.svg'const CardQ = ({ type,category,sub_date,sub_user,title,content,fav,fit,onClick }) => {    const info = {    lost:'迷子',    find: '目撃',    prot: '保護',    note: '記事',  }  const clr = (category==='受付中')?'var(--clr-qa)':'#9E9E9E'  const clrSub = (category==='受付中')?'var(--clr-qa-sub)':'#ccc'    const RenderItem =(icon,info)=>(    <div className={s.iconItem}>      <div className={s.icon}>        <img src={icon} />      </div>      <span>{info}</span>    </div>  )  // console.log(content.rep,'cnt')    return (    <div className={s.cardq} style={{color:clr}} onClick={onClick}>      <div className={s.hd} style={{background:clrSub}}>        <label style={{background:clr}}>{category}</label>        <i>回答</i>        <span>{content.rep.length}</span>      </div>      <div className={s.bd}>        <div className={s.tl}>          <h1>{title}</h1>        </div>        <p>{content.cnt}</p>        <div className={s.ft}>          {RenderItem(icon_user,sub_user)}          {RenderItem(icon_eye,fav.length)}          {sub_date}        </div>      </div>          </div>  )}export default observer(CardQ)