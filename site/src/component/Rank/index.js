import React,{useState,useEffect} from 'react';import { observer,MobXProviderContext } from 'mobx-react'import s from './index.module.less';import icon_ord   from '@/img/icon/ord.svg'const Rank = ({ id }) => {  let filter;  switch(id) {    case 1: filter='var(--fil-gold)';break;    case 2: filter='var(--fil-silver)';break;    case 3: filter='var(--fil-copper)';break;    case 4: filter='var(--fil-four)';break;    default: filter='none';  }  return (    <>      {(id) &&      <div className={s.rank}>        <span>{id}</span>        <img src={icon_ord} style={{filter: filter}} />      </div>}    </>  )}export default observer(Rank)